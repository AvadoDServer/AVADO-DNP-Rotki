version: '3.4'
services:
  rotki.avado.dnp.dappnode.eth:
    image: 'rotki.avado.dnp.dappnode.eth:0.0.1'
    build:
      context: ./build
      args:
        VERSION: v1.22.2
    volumes:
      - 'data:/data'
      - 'logs:/logs'
    ports:
      - '8084:80'
    environment:
      - EXTRA_OPTS=
volumes:
  data: {}
  logs: {}
